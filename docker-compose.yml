services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: learnself
      POSTGRES_USER: learnself
      POSTGRES_PASSWORD: learnself
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
